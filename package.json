{
  "name": "mesmerizer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.27.0",
    "vite": "^7.3.1",
    "wrangler": "^4.72.0"
  },
  "dependencies": {
    "gsap": "^3.14.2",
    "p5": "^2.2.2"
  }
}
